# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on Vercel

# Optional: set to "true" to skip server env validation (useful for tests)
# SKIP_ENV_VALIDATION=false

# Instructions to create an AI Gateway API key here: https://vercel.com/ai-gateway 
# API key required for non-Vercel deployments
# For Vercel deployments, OIDC tokens are used automatically
# https://vercel.com/ai-gateway
AI_GATEWAY_API_KEY=****


# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/postgres
POSTGRES_URL=****


# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****

# =============================================================================
# TWILIO WHATSAPP CONFIGURATION
# =============================================================================

# Required: Account credentials from https://console.twilio.com/
TWILIO_ACCOUNT_SID=****
TWILIO_AUTH_TOKEN=****

# Required: The public URL where Twilio sends webhook callbacks
# Must match exactly what's configured in Twilio Console or Messaging Service
TWILIO_WHATSAPP_WEBHOOK_URL=https://your-domain.com/api/whatsapp

# Choose ONE of the following for outbound messages:

# Option A (Recommended): Messaging Service SID
# Create at https://console.twilio.com/us1/develop/sms/services
# Provides sender pooling, smart routing, and centralized configuration
TWILIO_MESSAGING_SERVICE_SID=****

# Option B: Direct WhatsApp sender number in E.164 format
# Only used if TWILIO_MESSAGING_SERVICE_SID is not set
# TWILIO_WHATSAPP_FROM=+15551234567

# Optional: Agent identity for typing indicators via Conversations API
# This identity must be added as a participant in each conversation
# See: https://www.twilio.com/docs/conversations
TWILIO_CONVERSATIONS_AGENT_IDENTITY=****

# Optional: Content Template SID for WhatsApp buttons
# Create templates at https://console.twilio.com/us1/develop/sms/content-template-builder
# Templates with quick reply buttons can be used in-session without approval
# Call-to-action and coupon templates require WhatsApp approval
TWILIO_WHATSAPP_BUTTONS_CONTENT_SID=****
